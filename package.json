{
  "name": "living-survey",
  "version": "1.0.0",
  "private": true,
  "description": "A living literature survey that lives in your README and updates itself.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "update": "node scripts/update.js",
    "refresh": "node scripts/update.js --refresh",
    "test": "node --test --test-timeout=15000 \"test/**/*.test.js\""
  },
  "license": "MIT"
}
